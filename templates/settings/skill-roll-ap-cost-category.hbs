<section class="tab {{tab.cssClass}}" data-group="primary" data-tab="{{tab.id}}">
  <p class="hint">{{localize 'SKSK.SkillRollApCostConfig.Hint'}}</p>

  <ol class="items-list">
    {{#each skills as |skill|}}
    <li class="item flexcol skill-fp-panel">
      <label class="resource-label">{{localize skill.label}}</label>
      <div class="grid grid-3col">
        {{#each skill.fields as |field|}}
        <div class="grid-span-1">
          <label class="resource-label">{{field.label}}</label>
          <input type="text" name="skills.{{skill.key}}.{{field.key}}" value="{{field.value}}"
            data-dtype="Number" placeholder="2" />
        </div>
        {{/each}}
      </div>
    </li>
    {{/each}}
  </ol>
</section>
